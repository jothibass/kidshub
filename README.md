KidsHub - Full Deployable Bundle
This bundle is a demo monorepo (frontend + backend) ready to be pushed to GitHub and deployed to an EC2 instance via GitHub Actions.

See .github/workflows/deploy.yml for the deploy pipeline.
